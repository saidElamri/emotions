# sentiment-backend/pytest.ini

[pytest]
# Add the project root to the Python path.
# This allows pytest to find and import modules from the 'app' directory,
# as if the tests were being run from the project root.
pythonpath = .

# Tell pytest to use asyncio for all tests
# This is an alternative to adding @pytest.mark.asyncio to each test function
